from .caches import *


"""
    Features to add.

    - pop(), like delete(), but returns the value that was deleted.

    - replace(), like add(), but only sets the key if it already exists.

    - __iter__(): Iterate over keys

    - __len__: Return length of keys.


"""
